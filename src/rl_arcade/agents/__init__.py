"""Single-file-agent implementations.

Each algorithm lives in one module to stay readable (CleanRL-style).  The
P0 scaffold ships stubs that raise :class:`NotImplementedError`; real
implementations land in P1 (PPO), P2 (DQN), and P3 (SAC).
"""
